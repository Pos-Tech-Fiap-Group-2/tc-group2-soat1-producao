apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tech-challenge-group2-pedidos-db-pvc
  namespace: 'tech-challenge-group2-pedidos'
spec:
    storageClassName: manual
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
          storage: 1Gi